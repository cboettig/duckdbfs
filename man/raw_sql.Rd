% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_sql.R
\name{raw_sql}
\alias{raw_sql}
\title{Run a raw SQL query and return a lazy table}
\usage{
raw_sql(query, tblname = NULL, conn = cached_connection())
}
\arguments{
\item{query}{A character string of SQL to execute against \code{conn}.}

\item{tblname}{Optional name for a temporary view. If \code{NULL} (default),
the query is used as an inline subquery and nothing is persisted.
If supplied, a \verb{CREATE OR REPLACE TEMPORARY VIEW <tblname>} is created,
which can then be referenced by name in subsequent queries.}

\item{conn}{A duckdb connection, by default \code{\link[=cached_connection]{cached_connection()}}.}
}
\value{
A lazy \code{dplyr::tbl} object that can be further refined with
\code{dplyr} verbs or collected into memory with \code{\link[dplyr:compute]{dplyr::collect()}}.
}
\description{
An escape hatch for SQL that can't easily be expressed through the
\code{dplyr}/\code{dbplyr} translation layer, analogous to \code{ibis}'s \code{con.sql()}.
Useful for DuckDB-specific syntax such as \verb{UNION ALL BY NAME}, which
aligns columns by name across queries with differing schemas and fills
missing columns with NULL.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Combine two datasets with differing schemas using UNION ALL BY NAME
base <- paste0("https://github.com/duckdb/duckdb/raw/main/",
              "data/parquet-testing/hive-partitioning/union_by_name/")
a <- open_dataset(paste0(base, "x=1/f1.parquet"), tblname = "a")
b <- open_dataset(paste0(base, "x=2/f2.parquet"), tblname = "b")

raw_sql("SELECT * FROM a UNION ALL BY NAME SELECT * FROM b")
\dontshow{\}) # examplesIf}
}
